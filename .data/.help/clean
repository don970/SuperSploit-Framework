# 🧹 COMMAND: clean
Description: Performs a full cleanup of the framework environment. This truncates and clears history files, error logs, nmap scans, target history, gathered credentials, and other locally cached loot.

Usage:
    clean
